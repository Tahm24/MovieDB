<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommended Movies</title>
    <link rel="stylesheet" href="/public/favourites.css">
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">MovieDB</a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/favourites">Favourites</a></li>
                <li><a href="/recommended">Recommended</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        <h1>Recommended Movies</h1>

        <% if (movies && movies.length > 0) { %>
            <div class="movies-container">
                <% movies.forEach(movie => { %>
                    <div class="movie-card">
                        <img src="<%= movie.poster %>" alt="<%= movie.title %>">
                        <h3><%= movie.title %></h3>
                        <p>Release Date: <%= movie.release_date %></p>
                        <!-- Safely display avg_rating with fallback -->
                        <p>Average Rating: <%= parseFloat(movie.avg_rating) ? parseFloat(movie.avg_rating).toFixed(2) : "No Rating" %></p>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>No recommended movies at the moment. Rate some movies to get recommendations from favourites</p>
        <% } %>
    </div>
</body>
</html>
